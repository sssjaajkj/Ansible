- name: variable from files
  hosts: localhost
  tasks:
  - name: print information
    vars:
      timings: "4:23AM IST"
    ansible.builtin.debug:
      msg: "Hi , we are learing {{Course}} from Trainer {{Trainer}}, Duration  is {{Duration}},timings {{timings}}"
  